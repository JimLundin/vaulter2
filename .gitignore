node_modules/
dist/
# Frontend build output (Vite bundles web/ into public/; regenerated by `npm run build`).
public/
# Local dev vault used by `npm run dev` — a scratch folder or a symlink to a
# real vault (e.g. ~/my-vault). Never committed; the path stays out of the repo.
.dev-vault
*.log
